package ch02;

public class VarIntitTest {

	public static void main(String[] args) {

		int index = 1;
		
		index = index +1;
		System.out.println("index : " + index);
		
		/*int index;
		index = index +1; // 지역변수는 값의 초기화를 하지 않으면 인덱스 변수 초기화 에러가 난다.*/
	}

}
